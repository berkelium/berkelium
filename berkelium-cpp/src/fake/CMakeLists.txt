CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(berkelium-host-fake)

FILE(GLOB_RECURSE SRC "*.cpp")

ADD_DEFINITIONS(-DBERKELIUM_CPP_IMPL)
ADD_EXECUTABLE(host-fake ${SRC})
TARGET_LINK_LIBRARIES(host-fake ${BERKELIUM_CPP_LIBS} ${Boost_LIBRARIES})
